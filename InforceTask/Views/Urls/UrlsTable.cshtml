@model IEnumerable<InforceTask.Data.Entity.UrlsItem>

@{
    ViewData["Title"] = "Urls";
}

<table class="table table-striped table-hover">
    <thead class="table-light">
    <tr>
        @if (User.Identity?.Name is not null)
        {
            <app-item-add></app-item-add>
        }
    </tr>
    <tr>
        <th>
            Full Url
        </th>
        <th>
            Short Url
        </th>
        @if (User.Identity?.Name is not null)
        {
            <th>
                Actions
            </th>
        }
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FullUrl);
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShortUrl);
            </td>
            @if (User.Identity?.Name is not null)
            {
                <td>
                    <item-details item-id="item.Id"> </item-details>
                    @if (User.IsInRole("Administrators") || (bool)User.Identity?.Name.Equals(item.CreatedBy))
                    {
                        <item-delete item-id="item.Id"> </item-delete>
                    }
                </td>
            }
        </tr>
    }
    </tbody>
</table>
<script src="dist/ang-proj/runtime.e8f1f41829bceead.js" type="module"></script>
<script src="dist/ang-proj/polyfills.a09c9b979cea9413.js" type="module"></script>
<script src="dist/ang-proj/main.1e8159d5dac5cd34.js" type="module"></script>